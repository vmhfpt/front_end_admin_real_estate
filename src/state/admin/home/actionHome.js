/*import { actionThunk } from "./actionDemo";
import { createAsyncThunk } from "@reduxjs/toolkit/dist/createAsyncThunk";
export const actionChange = (value) => {
    return function demoThunk(dispatch, getState) {
      value ++;
      dispatch(actionThunk(value));
      console.log(getState(), value);
    }
}*/